package gr.charos.homeapp.commons.model.props;

public class Properties {
	
	public enum TransactionType {
		incoming, outgoing
	}
	
	public enum ExpenseType {
		fixed, dynamic
	}
	
	public enum IncomeType {
		wage, coupons, lottery, will,
	}

}
